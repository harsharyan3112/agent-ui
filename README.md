# Nexora Agent

Animated frontend-only agentic system prototype.

Run:
```bash
npm install
npm run dev -- -p 3001
```

The agent execution is mocked. Replace the `run()` simulation later with a backend request/SSE stream from your Spring Boot agent orchestrator.
